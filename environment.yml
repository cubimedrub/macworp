name: nf_cloud
channels:
  - defaults
dependencies:
  - libpq
  - nodejs=14.*
  - yarn
  - python=3.10
  - pip
  - setuptools
  - cython
  - yaml
  - pip:
    - -e ./backend[dev]
    - -e ./worker[dev]
